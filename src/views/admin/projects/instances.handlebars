<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Instâncias do Projeto - {{project.name}}</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="bg-light">
  
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Admin Dashboard</a>
      <div class="d-flex">
        <a href="/admin/logout" class="btn btn-outline-light">Sair</a>
      </div>
    </div>
  </nav>

  <div class="container mt-5">
    <h2>Instâncias do Projeto: {{project.name}}</h2>

    {{#if error}}
      <div class="alert alert-danger">{{error}}</div>
    {{/if}}

    <div class="list-group mt-3">
      {{#each instances}}
        <div class="list-group-item d-flex justify-content-between align-items-center">
          <div>
            <strong>{{this.name}}</strong> - {{this.code}} - {{this.status}}
          </div>
          <div>
            <!-- Correção no href para passar o project.id corretamente -->
            <a href="/admin/projects/{{../project.id}}/instances/{{this.id}}/edit" class="btn btn-sm btn-primary me-2">Editar</a>
            
            <!-- Botão de deletar instância -->
            <form action="/admin/projects/{{../project.id}}/instances/{{this.id}}/delete" method="POST" style="display: inline-block;">
              <button type="submit" class="btn btn-sm btn-danger">Deletar</button>
            </form>
          </div>
        </div>
      {{else}}
        <p>Nenhuma instância cadastrada para este projeto.</p>
      {{/each}}
    </div>

    <div class="mt-3">
      <!-- Botão de criar instância -->
      <a href="/admin/projects/{{project.id}}/instances/create" class="btn btn-success">+ Criar Nova Instância</a>
    </div>

  </div>

</body>
</html>
