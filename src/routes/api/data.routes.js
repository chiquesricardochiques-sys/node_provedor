// import { Router } from "express";
// import { getData, insertData } from "../../controllers/api/data.controller.js";

// const router = Router();

// router.get("/:project/:table", getData);
// router.post("/:project/:table", insertData);

// export default router;
